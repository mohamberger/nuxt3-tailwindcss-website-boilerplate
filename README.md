# Nuxt 3 + TailwindCSS Website Boilerplate

## Development Server

Start the development server

```bash
docker compose up app
```

## Setup

Make sure to install the dependencies:

```bash
docker compose run app ash

# yarn
yarn install
```
